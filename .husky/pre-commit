#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Validando código antes do commit..."

# Executar lint-staged (valida lint e formatação apenas nos arquivos staged)
npx lint-staged

# Validar build
echo "🔨 Validando build..."
npm run build

echo "✅ Validações concluídas!"
